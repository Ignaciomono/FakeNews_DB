# === VARIABLES DE ENTORNO PARA VERCEL DEPLOY ===
# Copia estas variables en tu dashboard de Vercel

# === BASE DE DATOS NEON (YA CONFIGURADAS) ===
# PGHOST=tu-host.neon.tech
# PGDATABASE=neondb 
# PGUSER=neondb_owner
# PGPASSWORD=tu-password
# PGSSLMODE=require

# === CONFIGURACIÓN GENERAL (AGREGAR ESTAS) ===
SECRET_KEY=fake-news-production-secret-key-2025-secure-xyz789
ENVIRONMENT=production
MAX_FILE_SIZE_MB=10

# === CORS PARA FRONTEND ===
CORS_ORIGINS=*

# === HUGGING FACE API ===
HF_API_URL=https://api-inference.huggingface.co/models/
HF_MODEL_NAME=cardiffnlp/twitter-roberta-base-sentiment-latest
HF_API_TOKEN=hf_TU_TOKEN_REAL_AQUI

# === CONFIGURACIÓN ADICIONAL (OPCIONALES) ===
REQUEST_TIMEOUT=30
MAX_CONTENT_LENGTH=50000
REQUESTS_PER_MINUTE=60

# === INSTRUCCIONES ===
# 1. Ve a tu proyecto en Vercel
# 2. Settings > Environment Variables
# 3. Agrega cada variable una por una
# 4. Redeploy tu proyecto

# === VARIABLES MÍNIMAS REQUERIDAS ===
# Las variables marcadas como "AGREGAR ESTAS" son obligatorias
# Las de base de datos ya las tienes configuradas
# El token HF_API_TOKEN mejorará significativamente el rendimiento